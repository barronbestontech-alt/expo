import * as React from 'react';
import { View, Text, StyleSheet, TouchableOpacity, ScrollView, Image } from 'react-native';
import { NavigationContainer } from '@react-navigation/native';
import { createStackNavigator } from '@react-navigation/stack';

const Stack = createStackNavigator();

function HomeScreen({ navigation }) {
  return (
    <ScrollView style={styles.container}>
      {/* Header */}
      <View style={styles.header}>
        <View style={styles.logoContainer}>
          <Image
            source={{
              uri: 'https://upload.wikimedia.org/wikipedia/commons/thumb/3/37/Church_dome_icon.svg/1200px-Church_dome_icon.svg.png',
            }}
            style={styles.logo}
          />
        </View>
        <Text style={styles.title}>The Sanctuary App</Text>
        <Text style={styles.subtitle}>“A home for faith in your hands.”</Text>
      </View>

      {/* Buttons */}
      <View style={styles.buttons}>
        <FeatureButton title="🎥 Sermons" onPress={() => navigation.navigate('Sermons')} />
        <FeatureButton title="📅 Events" onPress={() => navigation.navigate('Events')} />
        <FeatureButton title="💝 Giving" onPress={() => navigation.navigate('Giving')} />
        <FeatureButton title="🙏 Prayer Wall" onPress={() => navigation.navigate('Prayer Wall')} />
        <FeatureButton title="💬 Groups" onPress={() => navigation.navigate('Groups')} />
        <FeatureButton title="📖 Bible" onPress={() => navigation.navigate('Bible')} />
      </View>

      {/* Daily Verse */}
      <View style={styles.card}>
        <Text style={styles.cardTitle}>🌅 Daily Verse</Text>
        <Text style={styles.verse}>
          "The Lord is my shepherd, I shall not want."{'\n'}– Psalm 23:1
        </Text>
      </View>

      {/* Announcements */}
      <View style={styles.card}>
        <Text style={styles.cardTitle}>📣 Announcements</Text>
        <Text>- Sunday Service: 9:00 AM</Text>
        <Text>- Youth Worship Night: Friday 7:00 PM</Text>
      </View>
    </ScrollView>
  );
}

/* -------------- Simple Page Templates -------------- */
function SermonsScreen() {
  return (
    <ScreenTemplate title="🎥 Sermons" text="Watch past and live sermons here soon." />
  );
}

function EventsScreen() {
  return <ScreenTemplate title="📅 Events" text="Upcoming events will appear here." />;
}

function GivingScreen() {
  return (
    <ScreenTemplate
      title="💝 Giving"
      text="Give your tithes and offerings securely here."
    />
  );
}

function PrayerWallScreen() {
  return (
    <ScreenTemplate
      title="🙏 Prayer Wall"
      text="Share your prayer requests and testimonies here."
    />
  );
}

function GroupsScreen() {
  return <ScreenTemplate title="💬 Groups" text="Join ministry and fellowship groups." />;
}

function BibleScreen() {
  return <ScreenTemplate title="📖 Bible" text="Read the daily scripture here." />;
}

/* -------------- Component Templates -------------- */
function FeatureButton({ title, onPress }) {
  return (
    <TouchableOpacity style={styles.featureBtn} onPress={onPress}>
      <Text style={styles.featureText}>{title}</Text>
    </TouchableOpacity>
  );
}

function ScreenTemplate({ title, text }) {
  return (
    <View style={styles.screen}>
      <Text style={styles.pageTitle}>{title}</Text>
      <Text style={styles.pageText}>{text}</Text>
    </View>
  );
}

/* -------------- Main App -------------- */
export default function App() {
  return (
    <NavigationContainer>
      <Stack.Navigator
        screenOptions={{
          headerStyle: { backgroundColor: '#1E3A8A' },
          headerTintColor: '#fff',
          headerTitleStyle: { fontWeight: 'bold' },
        }}
      >
        <Stack.Screen name="Home" component={HomeScreen} />
        <Stack.Screen name="Sermons" component={SermonsScreen} />
        <Stack.Screen name="Events" component={EventsScreen} />
        <Stack.Screen name="Giving" component={GivingScreen} />
        <Stack.Screen name="Prayer Wall" component={PrayerWallScreen} />
        <Stack.Screen name="Groups" component={GroupsScreen} />
        <Stack.Screen name="Bible" component={BibleScreen} />
      </Stack.Navigator>
    </NavigationContainer>
  );
}

/* -------------- Styles -------------- */
const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#FFFFFF',
    paddingTop: 40,
  },
  header: {
    alignItems: 'center',
    paddingBottom: 20,
    backgroundColor: '#1E3A8A',
    borderBottomLeftRadius: 40,
    borderBottomRightRadius: 40,
  },
  logoContainer: {
    backgroundColor: '#FFFFFF',
    borderRadius: 50,
    padding: 10,
    marginTop: 20,
  },
  logo: {
    width: 80,
    height: 80,
  },
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    marginTop: 10,
    color: '#FFFFFF',
  },
  subtitle: {
    fontSize: 14,
    color: '#E2E8F0',
    fontStyle: 'italic',
  },
  buttons: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'center',
    marginVertical: 15,
  },
  featureBtn: {
    backgroundColor: '#E0ECFF',
    borderRadius: 20,
    paddingVertical: 10,
    paddingHorizontal: 20,
    margin: 6,
  },
  featureText: {
    fontSize: 16,
    color: '#1E3A8A',
    fontWeight: '500',
  },
  card: {
    backgroundColor: '#F8FAFC',
    marginHorizontal: 15,
    marginVertical: 8,
    borderRadius: 20,
    padding: 20,
    shadowColor: '#000',
    shadowOpacity: 0.05,
    shadowRadius: 5,
    elevation: 1,
  },
  cardTitle: {
    fontWeight: 'bold',
    fontSize: 18,
    color: '#1E3A8A',
    marginBottom: 10,
  },
  verse: {
    fontSize: 16,
    color: '#1E293B',
    fontStyle: 'italic',
  },
  screen: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '#FFFFFF',
    padding: 20,
  },
  pageTitle: {
    fontSize: 22,
    fontWeight: 'bold',
    color: '#1E3A8A',
    marginBottom: 10,
  },
  pageText: {
    fontSize: 16,
    color: '#1E293B',
    textAlign: 'center',
  },
});
